<UserControl x:Class="PerfectWorldManager.Gui.CharacterEditorViewThemed"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PerfectWorldManager.Gui"
             xmlns:vm="clr-namespace:PerfectWorldManager.Gui.ViewModels;assembly=PerfectWorldManager.Gui"
             xmlns:models="clr-namespace:PerfectWorldManager.Core.Models;assembly=PerfectWorldManager.Core"
             xmlns:converters="clr-namespace:PerfectWorldManager.Gui.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CharacterEditorViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="700" d:DesignWidth="900">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <converters:StringPathToImageSourceConverter x:Key="PathToImageConverter"/>
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <converters:BooleanToHeightConverter x:Key="BooleanToHeightConverterForTextArea" TrueHeight="80" FalseHeight="32"/>
            <converters:NullToBooleanConverter x:Key="NullToTrueConverter"/>
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
            
            <!-- Inventory Item Style -->
            <Style x:Key="InventoryItemStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource ModernEvenLighterBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource ModernBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="Margin" Value="2"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Trigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color"
                                                    To="{StaticResource ModernAccentColor}"
                                                    Duration="{StaticResource FastAnimationDuration}"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                     To="1.1" Duration="{StaticResource FastAnimationDuration}"
                                                     EasingFunction="{StaticResource StandardEase}"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                     To="1.1" Duration="{StaticResource FastAnimationDuration}"
                                                     EasingFunction="{StaticResource StandardEase}"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                        <Trigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color"
                                                    To="{StaticResource ModernBorderColor}"
                                                    Duration="{StaticResource FastAnimationDuration}"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                     To="1" Duration="{StaticResource FastAnimationDuration}"
                                                     EasingFunction="{StaticResource StandardEase}"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                     To="1" Duration="{StaticResource FastAnimationDuration}"
                                                     EasingFunction="{StaticResource StandardEase}"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.ExitActions>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- Empty Slot Style -->
            <Style x:Key="EmptySlotStyle" TargetType="Border">
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#10FFFFFF" Offset="0"/>
                            <GradientStop Color="#05FFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="BorderBrush" Value="#20FFFFFF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="Margin" Value="2"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="Opacity" Value="0.5"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Background="{StaticResource ModernBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Controls -->
            <Border Grid.Row="0" Style="{StaticResource ModernCard}" Margin="0,0,0,4" Padding="16">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="{DynamicResource CharacterEditor_CharacterIDLabel}" 
                               VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding CharacterId, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="120" Margin="5,0" VerticalAlignment="Center"/>
                        <Button Content="{DynamicResource CharacterEditor_LoadCharacterButton}" 
                                Command="{Binding LoadCharacterCommand}" 
                                Margin="5,0" 
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                        <Button Content="{DynamicResource CharacterEditor_RefreshCharacterButton}" 
                                Command="{Binding RefreshCharacterCommand}" 
                                Margin="5,0" 
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                        <Button Content="{DynamicResource CharacterEditor_SaveCharacterButton}" 
                                Command="{Binding SaveCharacterCommand}" 
                                Margin="5,0" 
                                Style="{StaticResource AccentButtonStyle}"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                        <TextBlock Text="{Binding StatusMessage}" 
                                   Margin="15,0" 
                                   VerticalAlignment="Center" 
                                   Foreground="{StaticResource ModernWarningBrush}" 
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="{DynamicResource CharacterEditor_SyncGuiToXmlButton}" 
                                Command="{Binding SyncGuiToXmlCommand}" 
                                Margin="5,0" 
                                ToolTip="Updates the Raw XML view from any changes made in the GUI Editor." 
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                        <Button Content="{DynamicResource CharacterEditor_SyncXmlToGuiButton}" 
                                Command="{Binding SyncXmlToGuiCommand}" 
                                Margin="5,0" 
                                ToolTip="Reloads the GUI Editor from the content of the Raw XML view." 
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Main Tab Control -->
            <TabControl Grid.Row="1" Name="EditorModeTabControl" 
                        SelectionChanged="EditorModeTabControl_SelectionChanged"
                        Background="Transparent">
                <TabItem Header="{DynamicResource CharacterEditor_TabGuiEditor}" x:Name="GuiEditorTab">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" 
                                  Background="{StaticResource ModernBackgroundBrush}">
                        <Grid DataContext="{Binding CurrentCharacterVm}" Margin="8">
                            <TabControl Visibility="{Binding Converter={StaticResource NullToVisibilityConverter}}"
                                        Background="Transparent">
                                <!-- Base Info Tab -->
                                <TabItem Header="{DynamicResource CharacterEditor_TabBaseInfo}">
                                    <Border Style="{StaticResource ModernCard}">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding BaseInfo.Variables}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="{x:Type models:CharacterVariableVm}">
                                                        <Border BorderBrush="{StaticResource ModernBorderBrush}" 
                                                                BorderThickness="0,0,0,1" 
                                                                Padding="0,8">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="200"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Label Grid.Column="0" 
                                                                       Content="{Binding Name, StringFormat='{}{0}:'}" 
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{StaticResource ModernSecondaryTextBrush}"/>
                                                                <TextBox Grid.Column="1" 
                                                                         Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         MinWidth="300" MaxWidth="500"
                                                                         AcceptsReturn="{Binding IsLongText}"
                                                                         TextWrapping="Wrap"
                                                                         Height="{Binding IsLongText, Converter={StaticResource BooleanToHeightConverterForTextArea}}"
                                                                         VerticalAlignment="Top"/>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </TabItem>
                                
                                <!-- Status Info Tab -->
                                <TabItem Header="{DynamicResource CharacterEditor_TabStatusInfo}">
                                    <Border Style="{StaticResource ModernCard}">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding StatusInfo.Variables}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="{x:Type models:CharacterVariableVm}">
                                                        <Border BorderBrush="{StaticResource ModernBorderBrush}" 
                                                                BorderThickness="0,0,0,1" 
                                                                Padding="0,8">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="200"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Label Grid.Column="0" 
                                                                       Content="{Binding Name, StringFormat='{}{0}:'}" 
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{StaticResource ModernSecondaryTextBrush}"/>
                                                                <TextBox Grid.Column="1" 
                                                                         Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         MinWidth="300" MaxWidth="500"
                                                                         AcceptsReturn="{Binding IsLongText}"
                                                                         TextWrapping="Wrap"
                                                                         Height="{Binding IsLongText, Converter={StaticResource BooleanToHeightConverterForTextArea}}"
                                                                         VerticalAlignment="Top"/>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </TabItem>
                                
                                <!-- Inventory Tab -->
                                <TabItem Header="{DynamicResource CharacterEditor_TabInventoryAndSafe}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" MinWidth="500"/>
                                            <ColumnDefinition Width="350"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Inventory Sections -->
                                        <ScrollViewer Grid.Column="0" 
                                                      VerticalScrollBarVisibility="Auto" 
                                                      HorizontalScrollBarVisibility="Disabled">
                                            <StackPanel Margin="8">
                                                <!-- Equipment Section -->
                                                <Border Style="{StaticResource ModernCard}" Margin="0,0,0,8">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource CharacterEditor_EquipmentLabel}" 
                                                                   FontSize="16" 
                                                                   FontWeight="SemiBold" 
                                                                   Foreground="{StaticResource ModernAccentBrush}"
                                                                   Margin="0,0,0,12"/>
                                                        <ItemsControl ItemsSource="{Binding Equipment.Items}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel Orientation="Horizontal"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate DataType="{x:Type models:InventoryItemVm}">
                                                                    <Border Style="{StaticResource InventoryItemStyle}">
                                                                        <Grid>
                                                                            <TextBlock Text="?" 
                                                                                       HorizontalAlignment="Center" 
                                                                                       VerticalAlignment="Center" 
                                                                                       Foreground="{StaticResource ModernSecondaryTextBrush}" 
                                                                                       FontSize="20"
                                                                                       Opacity="0.3"/>
                                                                            <Image Source="{Binding IconPath, Converter={StaticResource PathToImageConverter}}" 
                                                                                   ToolTip="{Binding ItemName}" 
                                                                                   Cursor="Hand"
                                                                                   Margin="4">
                                                                                <Image.InputBindings>
                                                                                    <MouseBinding MouseAction="LeftClick" 
                                                                                                  Command="{Binding DataContext.SelectInventoryItemCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                                                                  CommandParameter="{Binding}"/>
                                                                                </Image.InputBindings>
                                                                            </Image>
                                                                        </Grid>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>

                                                <!-- Pocket Section -->
                                                <Border Style="{StaticResource ModernCard}" Margin="0,0,0,8">
                                                    <StackPanel>
                                                        <Grid Margin="0,0,0,12">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" 
                                                                       Text="{DynamicResource CharacterEditor_PocketItemsLabel}" 
                                                                       FontSize="16" 
                                                                       FontWeight="SemiBold" 
                                                                       Foreground="{StaticResource ModernAccentBrush}"/>
                                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                                <TextBlock Text="{DynamicResource CharacterEditor_PocketMoneyLabel}" 
                                                                           VerticalAlignment="Center" 
                                                                           Margin="0,0,8,0" 
                                                                           FontWeight="SemiBold"/>
                                                                <TextBox Width="120"
                                                                         Text="{Binding PocketInfo.Money, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         IsEnabled="{Binding DataContext.CurrentCharacterVm, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Converter={StaticResource NullToTrueConverter}}"/>
                                                            </StackPanel>
                                                        </Grid>
                                                        <ItemsControl ItemsSource="{Binding PocketInfo.Items}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel Orientation="Horizontal"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate DataType="{x:Type models:InventoryItemVm}">
                                                                    <Border Style="{StaticResource InventoryItemStyle}">
                                                                        <Grid>
                                                                            <TextBlock Text="?" 
                                                                                       HorizontalAlignment="Center" 
                                                                                       VerticalAlignment="Center" 
                                                                                       Foreground="{StaticResource ModernSecondaryTextBrush}" 
                                                                                       FontSize="20"
                                                                                       Opacity="0.3"/>
                                                                            <Image Source="{Binding IconPath, Converter={StaticResource PathToImageConverter}}" 
                                                                                   ToolTip="{Binding ItemName}" 
                                                                                   Cursor="Hand"
                                                                                   Margin="4">
                                                                                <Image.InputBindings>
                                                                                    <MouseBinding MouseAction="LeftClick" 
                                                                                                  Command="{Binding DataContext.SelectInventoryItemCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                                                                  CommandParameter="{Binding}"/>
                                                                                </Image.InputBindings>
                                                                            </Image>
                                                                        </Grid>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <!-- Empty slots visualization -->
                                                        <ItemsControl>
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel Orientation="Horizontal"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.Items>
                                                                <Border Style="{StaticResource EmptySlotStyle}"/>
                                                                <Border Style="{StaticResource EmptySlotStyle}"/>
                                                                <Border Style="{StaticResource EmptySlotStyle}"/>
                                                                <Border Style="{StaticResource EmptySlotStyle}"/>
                                                            </ItemsControl.Items>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>

                                                <!-- Storehouse Section -->
                                                <Border Style="{StaticResource ModernCard}">
                                                    <StackPanel>
                                                        <Grid Margin="0,0,0,12">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" 
                                                                       Text="{DynamicResource CharacterEditor_StorehouseItemsLabel}" 
                                                                       FontSize="16" 
                                                                       FontWeight="SemiBold" 
                                                                       Foreground="{StaticResource ModernAccentBrush}"/>
                                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                                <TextBlock Text="{DynamicResource CharacterEditor_StorehouseMoneyLabel}" 
                                                                           VerticalAlignment="Center" 
                                                                           Margin="0,0,8,0" 
                                                                           FontWeight="SemiBold"/>
                                                                <TextBox Width="120"
                                                                         Text="{Binding StorehouseInfo.Money, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         IsEnabled="{Binding DataContext.CurrentCharacterVm, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Converter={StaticResource NullToTrueConverter}}"/>
                                                            </StackPanel>
                                                        </Grid>
                                                        <ItemsControl ItemsSource="{Binding StorehouseInfo.Items}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel Orientation="Horizontal"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate DataType="{x:Type models:InventoryItemVm}">
                                                                    <Border Style="{StaticResource InventoryItemStyle}">
                                                                        <Grid>
                                                                            <TextBlock Text="?" 
                                                                                       HorizontalAlignment="Center" 
                                                                                       VerticalAlignment="Center" 
                                                                                       Foreground="{StaticResource ModernSecondaryTextBrush}" 
                                                                                       FontSize="20"
                                                                                       Opacity="0.3"/>
                                                                            <Image Source="{Binding IconPath, Converter={StaticResource PathToImageConverter}}" 
                                                                                   ToolTip="{Binding ItemName}" 
                                                                                   Cursor="Hand"
                                                                                   Margin="4">
                                                                                <Image.InputBindings>
                                                                                    <MouseBinding MouseAction="LeftClick" 
                                                                                                  Command="{Binding DataContext.SelectInventoryItemCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                                                                  CommandParameter="{Binding}"/>
                                                                                </Image.InputBindings>
                                                                            </Image>
                                                                        </Grid>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                        </ScrollViewer>

                                        <!-- Item Details Panel -->
                                        <Border Grid.Column="1" 
                                                Style="{StaticResource ModernCard}"
                                                Margin="8,8,8,8"
                                                Visibility="{Binding DataContext.SelectedInventoryItem, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Converter={StaticResource NullToVisibilityConverter}}">
                                            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                                          DataContext="{Binding DataContext.SelectedInventoryItem, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding ItemName}" 
                                                               FontWeight="Bold" 
                                                               FontSize="18" 
                                                               Margin="0,0,0,16" 
                                                               Foreground="{StaticResource ModernAccentBrush}"
                                                               TextWrapping="Wrap"/>
                                                    <ItemsControl ItemsSource="{Binding Variables}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate DataType="{x:Type models:CharacterVariableVm}">
                                                                <Border BorderBrush="{StaticResource ModernBorderBrush}" 
                                                                        BorderThickness="0,0,0,1" 
                                                                        Padding="0,8">
                                                                    <StackPanel>
                                                                        <Label Content="{Binding Name, StringFormat='{}{0}:'}" 
                                                                               Padding="0,0,0,4" 
                                                                               FontWeight="SemiBold"
                                                                               Foreground="{StaticResource ModernSecondaryTextBrush}"/>
                                                                        <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                                 AcceptsReturn="{Binding IsLongText}" 
                                                                                 TextWrapping="Wrap"
                                                                                 Height="{Binding IsLongText, Converter={StaticResource BooleanToHeightConverterForTextArea}}"/>
                                                                    </StackPanel>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </StackPanel>
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </TabItem>
                                
                                <!-- Task Info Tab -->
                                <TabItem Header="{DynamicResource CharacterEditor_TabTaskInfo}">
                                    <Border Style="{StaticResource ModernCard}">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding TaskInfo.Variables}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="{x:Type models:CharacterVariableVm}">
                                                        <Border BorderBrush="{StaticResource ModernBorderBrush}" 
                                                                BorderThickness="0,0,0,1" 
                                                                Padding="0,8">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="200"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Label Grid.Column="0" 
                                                                       Content="{Binding Name, StringFormat='{}{0}:'}" 
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{StaticResource ModernSecondaryTextBrush}"/>
                                                                <TextBox Grid.Column="1" 
                                                                         Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         MinWidth="300" MaxWidth="500"
                                                                         AcceptsReturn="{Binding IsLongText}"
                                                                         TextWrapping="Wrap"
                                                                         Height="{Binding IsLongText, Converter={StaticResource BooleanToHeightConverterForTextArea}}"
                                                                         VerticalAlignment="Top"/>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </TabItem>
                            </TabControl>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
                
                <!-- Raw XML Tab -->
                <TabItem Header="{DynamicResource CharacterEditor_TabRawXmlEditor}" x:Name="RawXmlEditorTab">
                    <Border Style="{StaticResource ModernCard}" Margin="8">
                        <TextBox Text="{Binding CharacterXml, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" AcceptsTab="True"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="13"
                                 Background="{StaticResource ModernSurfaceBrush}" 
                                 Foreground="{StaticResource ModernForegroundBrush}" 
                                 BorderThickness="0"
                                 Padding="12"/>
                    </Border>
                </TabItem>

                <!-- Player Characters Tab -->
                <TabItem Header="{DynamicResource CharacterEditor_TabPlayerCharacters}" x:Name="PlayerCharactersTab" 
                         IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Search by User ID Section -->
                        <Border Grid.Column="0" Style="{StaticResource ModernCard}" Margin="8,8,4,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Search by User ID" 
                                           FontSize="16" FontWeight="SemiBold" 
                                           Margin="0,0,0,16"
                                           Foreground="{StaticResource ModernAccentBrush}"/>

                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="{DynamicResource CharacterEditor_PlayerUserIDLabel}" 
                                               VerticalAlignment="Center" 
                                               Margin="0,0,8,0"
                                               FontWeight="SemiBold"/>
                                    <TextBox Text="{Binding SearchPlayerIdText, UpdateSourceTrigger=PropertyChanged}" 
                                             MinWidth="120" 
                                             VerticalAlignment="Center"
                                             ToolTipService.ToolTip="Enter the numeric User ID of the player."/>
                                    <Button Content="{DynamicResource CharacterEditor_SearchCharactersButton}" 
                                            Command="{Binding SearchPlayerCharactersCommand}" 
                                            Margin="12,0,0,0" 
                                            Style="{StaticResource AccentButtonStyle}"/>
                                </StackPanel>

                                <TextBlock Grid.Row="2" 
                                           Text="{Binding PlayerCharactersStatusMessage}" 
                                           Margin="0,0,0,12" 
                                           FontStyle="Italic" 
                                           TextWrapping="Wrap"
                                           Foreground="{StaticResource ModernSecondaryTextBrush}"/>

                                <DataGrid Grid.Row="3" 
                                          ItemsSource="{Binding PlayerCharactersList}" 
                                          AutoGenerateColumns="False" 
                                          IsReadOnly="True" 
                                          CanUserAddRows="False"
                                          HeadersVisibility="Column" 
                                          VerticalScrollBarVisibility="Auto" 
                                          HorizontalScrollBarVisibility="Auto"
                                          Background="{StaticResource ModernSurfaceBrush}"
                                          BorderThickness="0">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="{DynamicResource CharacterEditor_ColCharID}" 
                                                            Binding="{Binding RoleId}" 
                                                            Width="Auto" 
                                                            MinWidth="120"/>
                                        <DataGridTextColumn Header="{DynamicResource CharacterEditor_ColCharName}" 
                                                            Binding="{Binding RoleName}" 
                                                            Width="*" 
                                                            MinWidth="150"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <!-- All Characters List Section -->
                        <Border Grid.Column="1" Style="{StaticResource ModernCard}" Margin="4,8,8,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="All Characters (ID: 1024 - 9000000)" 
                                               FontSize="16" FontWeight="SemiBold" 
                                               Foreground="{StaticResource ModernAccentBrush}"/>
                                    <TextBlock Grid.Column="1" 
                                               Text="{Binding AllCharactersStatusMessage}" 
                                               FontStyle="Italic" 
                                               Foreground="{StaticResource ModernSecondaryTextBrush}"/>
                                </Grid>

                                <DataGrid Grid.Row="1" 
                                          ItemsSource="{Binding AllCharactersList}" 
                                          AutoGenerateColumns="False" 
                                          IsReadOnly="True" 
                                          CanUserAddRows="False"
                                          HeadersVisibility="Column" 
                                          VerticalScrollBarVisibility="Auto" 
                                          HorizontalScrollBarVisibility="Auto"
                                          Background="{StaticResource ModernSurfaceBrush}"
                                          BorderThickness="0">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" 
                                                            Binding="{Binding RoleId}" 
                                                            Width="80"/>
                                        <DataGridTextColumn Header="Name" 
                                                            Binding="{Binding RoleName}" 
                                                            Width="150"/>
                                        <DataGridTextColumn Header="Level" 
                                                            Binding="{Binding Level}" 
                                                            Width="60"/>
                                        <DataGridTextColumn Header="User ID" 
                                                            Binding="{Binding UserId}" 
                                                            Width="80"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!-- Pagination Controls -->
                                <Border Grid.Row="2" 
                                        Background="{StaticResource ModernSurfaceBrush}" 
                                        Padding="12,8"
                                        Margin="-1,0,-1,-1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                                            <TextBlock Text="Page " VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding CurrentPage}" 
                                                       FontWeight="SemiBold" 
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Text=" of " VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding TotalPages}" 
                                                       FontWeight="SemiBold" 
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <Button Content="◀ Previous" 
                                                    Command="{Binding PreviousPageCommand}"
                                                    IsEnabled="{Binding CanGoPrevious}"
                                                    Margin="0,0,8,0"
                                                    MinWidth="90"/>
                                            <Button Content="Next ▶" 
                                                    Command="{Binding NextPageCommand}"
                                                    IsEnabled="{Binding CanGoNext}"
                                                    MinWidth="90"/>
                                            <Button Content="Refresh" 
                                                    Command="{Binding RefreshAllCharactersCommand}"
                                                    Margin="16,0,0,0"
                                                    Style="{StaticResource AccentButtonStyle}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>

            </TabControl>
        </Grid>

        <!-- Loading Overlay -->
        <Border Background="#CC000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
                Grid.RowSpan="2">
            <Border Style="{StaticResource ModernCard}" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    MinWidth="300">
                <StackPanel>
                    <TextBlock Text="{DynamicResource CharacterEditor_LoadingText}" 
                               FontSize="18" 
                               Margin="0,0,0,16"
                               HorizontalAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" 
                                 Width="250" 
                                 Height="4"
                                 Background="{StaticResource ModernEvenLighterBackgroundBrush}"
                                 Foreground="{StaticResource ModernAccentBrush}"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>